---
phase: 04-credentials-and-mobile
plan: 04
type: execute
wave: 3
depends_on: ["04-03"]
files_modified:
  - mobile/lib/core/models/didcomm_message.dart
  - mobile/lib/core/models/approval.dart
  - mobile/lib/core/models/credential.dart
  - mobile/lib/core/api/api_client.dart
  - mobile/lib/core/crypto/jws_service.dart
  - mobile/lib/features/onboarding/register_screen.dart
  - mobile/lib/features/onboarding/recovery_passphrase_screen.dart
  - mobile/lib/features/inbox/inbox_screen.dart
  - mobile/lib/features/inbox/approval_card.dart
  - mobile/lib/features/credentials/credentials_screen.dart
  - mobile/lib/features/approvals/approvals_screen.dart
  - mobile/lib/providers/inbox_provider.dart
  - mobile/lib/main.dart
  - mobile/pubspec.yaml
autonomous: true
requirements:
  - MOB-01
  - MOB-02
  - MOB-03
  - MOB-04
  - MOB-05
  - MOB-06

must_haves:
  truths:
    - "App generates Ed25519 keypair on first launch, stores private key in flutter_secure_storage"
    - "App registers a did:web DID with the server using the generated public key"
    - "After registration, app prompts user to set a recovery passphrase and stores passphrase-encrypted key backup"
    - "Inbox screen polls GET /v1/didcomm/inbox and displays DIDComm messages"
    - "Approval request messages render branded cards (with template) or fallback cards (without)"
    - "User can approve or decline with biometric confirmation, producing a JWS signature"
    - "Credentials screen displays entity's VCs from GET /v1/credentials"
    - "Approvals screen lists persistent approvals and allows revocation"
  artifacts:
    - path: "mobile/lib/core/models/didcomm_message.dart"
      provides: "DIDComm message model replacing old Signal model"
      contains: "class DIDCommMessage"
    - path: "mobile/lib/core/models/approval.dart"
      provides: "Approval model for approval cards"
      contains: "class Approval"
    - path: "mobile/lib/core/crypto/jws_service.dart"
      provides: "JWS signing service for approval responses"
      contains: "signDetached"
    - path: "mobile/lib/features/onboarding/register_screen.dart"
      provides: "Onboarding with keypair generation and DID registration"
      contains: "Ed25519"
    - path: "mobile/lib/features/onboarding/recovery_passphrase_screen.dart"
      provides: "Recovery passphrase setup screen (MOB-01)"
      contains: "RecoveryPassphraseScreen"
    - path: "mobile/lib/features/inbox/inbox_screen.dart"
      provides: "DIDComm inbox feed"
      contains: "DIDCommMessage"
    - path: "mobile/lib/features/inbox/approval_card.dart"
      provides: "Branded and fallback approval card renderer"
      contains: "ApprovalCard"
  key_links:
    - from: "mobile/lib/features/inbox/inbox_screen.dart"
      to: "mobile/lib/core/api/api_client.dart"
      via: "poll GET /v1/didcomm/inbox"
      pattern: "getInbox|didcomm/inbox"
    - from: "mobile/lib/features/inbox/approval_card.dart"
      to: "mobile/lib/core/crypto/jws_service.dart"
      via: "sign approval response on approve/decline"
      pattern: "signDetached|JwsService"
    - from: "mobile/lib/core/crypto/jws_service.dart"
      to: "mobile/lib/core/crypto/ed25519_service.dart"
      via: "Ed25519 signing with stored key"
      pattern: "ed25519|sign"
    - from: "mobile/lib/features/onboarding/register_screen.dart"
      to: "mobile/lib/features/onboarding/recovery_passphrase_screen.dart"
      via: "navigate to passphrase setup after DID registration"
      pattern: "RecoveryPassphraseScreen"
---

<objective>
Rebuild the Flutter mobile app around the DIDComm/approval architecture: new models (replacing old Signal), onboarding with Ed25519 key generation, DID registration, and recovery passphrase setup, DIDComm inbox with approval card rendering, biometric-gated JWS signing, credential management, and persistent approval management.

Purpose: The mobile app is the primary interface for human entities to manage approvals and credentials. The existing code references deleted Signal/SSE models and must be rewritten for DIDComm.
Output: Working Flutter app with onboarding (including recovery passphrase), inbox, approval cards, credential screen, approval management, and biometric signing.
</objective>

<execution_context>
@/Users/svenloth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/svenloth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-credentials-and-mobile/04-RESEARCH.md
@.planning/phases/04-credentials-and-mobile/04-01-SUMMARY.md
@.planning/phases/04-credentials-and-mobile/04-03-SUMMARY.md

@mobile/lib/main.dart
@mobile/lib/core/api/api_client.dart
@mobile/lib/core/crypto/ed25519_service.dart
@mobile/lib/core/crypto/secure_storage.dart
@mobile/lib/core/models/signal.dart
@mobile/lib/core/models/entity.dart
@mobile/lib/features/inbox/inbox_screen.dart
@mobile/lib/features/onboarding/register_screen.dart
@mobile/lib/providers/inbox_provider.dart
@mobile/pubspec.yaml
@spec/ATAP-SPEC-v1.0-rc1.md (Section 12: Mobile Client)

<interfaces>
<!-- Existing mobile code patterns -->

From mobile/lib/core/crypto/ed25519_service.dart:
```dart
// Expected: Ed25519 key generation and signing service
// Uses ed25519_edwards package
```

From mobile/lib/core/crypto/secure_storage.dart:
```dart
// Expected: flutter_secure_storage wrapper for key persistence
```

From mobile/lib/core/api/api_client.dart:
```dart
// Expected: HTTP client for ATAP API calls
// Needs updating: add DIDComm inbox, credential, approval endpoints
```

API endpoints to call:
- POST /v1/entities (register with public key)
- GET /v1/didcomm/inbox (DIDComm message polling)
- POST /v1/approvals/{id}/respond (approve/decline)
- GET /v1/credentials (list credentials)
- GET /v1/approvals (list approvals)
- DELETE /v1/approvals/{id} (revoke)
- POST /v1/oauth/token (DPoP token)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: New models, API client update, JWS service</name>
  <files>
    mobile/pubspec.yaml,
    mobile/lib/core/models/didcomm_message.dart,
    mobile/lib/core/models/approval.dart,
    mobile/lib/core/models/credential.dart,
    mobile/lib/core/api/api_client.dart,
    mobile/lib/core/crypto/jws_service.dart
  </files>
  <action>
    1. Update `pubspec.yaml`: add `local_auth: ^2.3.0` dependency (for biometric gate), add `encrypt: ^5.0.3` dependency (for passphrase-based key encryption). Verify `ed25519_edwards`, `flutter_secure_storage`, `http` already present.

    2. Create `core/models/didcomm_message.dart`:
       ```dart
       class DIDCommMessage {
           final String id;
           final String messageType;
           final String senderDID;
           final Map<String, dynamic> body;
           final DateTime createdAt;

           bool get isApprovalRequest =>
               messageType == 'https://atap.dev/protocols/approval/1.0/request' ||
               messageType == 'https://atap.dev/protocols/approval/1.0/cosigned';

           factory DIDCommMessage.fromJson(Map<String, dynamic> json) => ...
       }
       ```

    3. Create `core/models/approval.dart`:
       ```dart
       class Approval {
           final String id;
           final String from;
           final String to;
           final String? via;
           final String state;
           final ApprovalSubject subject;
           final String? templateUrl;
           final Map<String, String> signatures;
           final DateTime createdAt;
           final DateTime? validUntil;

           bool get isPersistent => validUntil != null;
           factory Approval.fromJson(Map<String, dynamic> json) => ...
       }

       class ApprovalSubject {
           final String type;
           final String label;
           final bool reversible;
           final Map<String, dynamic> payload;
           factory ApprovalSubject.fromJson(Map<String, dynamic> json) => ...
       }
       ```

    4. Create `core/models/credential.dart`:
       ```dart
       class Credential {
           final String id;
           final String type;
           final String jwt;
           final DateTime issuedAt;
           final DateTime? revokedAt;
           factory Credential.fromJson(Map<String, dynamic> json) => ...
       }
       ```

    5. Update `core/api/api_client.dart`:
       - Delete old SSE/signal methods (if any)
       - Add methods:
         - `Future<List<DIDCommMessage>> getInbox()` — GET /v1/didcomm/inbox with DPoP token
         - `Future<void> respondApproval(String id, String status, String signature)` — POST /v1/approvals/{id}/respond
         - `Future<List<Credential>> getCredentials()` — GET /v1/credentials
         - `Future<List<Approval>> getApprovals()` — GET /v1/approvals
         - `Future<void> revokeApproval(String id)` — DELETE /v1/approvals/{id}
         - `Future<Map<String, dynamic>> fetchTemplate(String url)` — GET template URL, verify JWS proof
       - All authenticated calls include DPoP proof header (use existing OAuth token logic or add DPoP support)

    6. Create `core/crypto/jws_service.dart`:
       - `static Future<String> signDetached(Uint8List privateKey, String payload)` — create JWS Compact Serialization with detached payload (RFC 7797): base64url(header) + ".." + base64url(signature). Header: `{"alg":"EdDSA","kid":"<entity_did>#<key_id>"}`. Sign with Ed25519 using ed25519_edwards.
       - `static Future<bool> verifyDetached(Uint8List publicKey, String jws, String payload)` — verify JWS signature

    7. Run `cd /Users/svenloth/dev/atap/mobile && flutter pub get` to install new dependencies.
  </action>
  <verify>
    <automated>cd /Users/svenloth/dev/atap/mobile && flutter analyze --no-fatal-infos</automated>
  </verify>
  <done>
    All new models parse JSON correctly. API client has all DIDComm/credential/approval methods. JWS service produces valid detached JWS. No analysis errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Onboarding (with recovery passphrase), inbox, approval cards, credential/approval screens</name>
  <files>
    mobile/lib/features/onboarding/register_screen.dart,
    mobile/lib/features/onboarding/recovery_passphrase_screen.dart,
    mobile/lib/features/inbox/inbox_screen.dart,
    mobile/lib/features/inbox/approval_card.dart,
    mobile/lib/features/credentials/credentials_screen.dart,
    mobile/lib/features/approvals/approvals_screen.dart,
    mobile/lib/providers/inbox_provider.dart,
    mobile/lib/main.dart
  </files>
  <action>
    1. **Onboarding (MOB-01)**: Update `register_screen.dart`:
       - On "Register" tap: generate Ed25519 keypair via ed25519_service, store private key in flutter_secure_storage, POST /v1/entities with type="human" and base64-encoded public key, receive DID back, store DID and entity ID in secure storage.
       - Show the generated DID to the user after registration.
       - After successful registration, navigate to `RecoveryPassphraseScreen` (see step 1b).
       - Delete old claim_screen.dart (references deleted claim model).

    1b. **Recovery Passphrase (MOB-01)**: Create `features/onboarding/recovery_passphrase_screen.dart`:
       - `RecoveryPassphraseScreen` — shown immediately after DID registration as part of onboarding flow.
       - UI: Text field for passphrase entry, confirm passphrase field, "Set Passphrase" button, "Skip for now" link.
       - On "Set Passphrase": validate passphrase >= 12 chars, derive AES-256 key from passphrase using PBKDF2 (use `encrypt` package with 100,000 iterations, random salt), encrypt the Ed25519 private key with derived key, store the encrypted backup + salt in flutter_secure_storage under key `recovery_backup`. Store a flag `has_recovery_passphrase: true` in secure storage.
       - On "Skip for now": store `has_recovery_passphrase: false`, navigate to main app. Show a reminder badge on Settings tab until passphrase is set.
       - After setting passphrase, navigate to main app (inbox screen).
       - This satisfies MOB-01 "set recovery passphrase" requirement. The passphrase enables future key recovery if the device is lost (recovery import flow is a future phase feature).

    2. **Inbox (MOB-02)**: Rewrite `inbox_screen.dart`:
       - Replace Signal model references with DIDCommMessage.
       - Use a polling mechanism: call `apiClient.getInbox()` every 15 seconds (or pull-to-refresh).
       - Display messages in a ListView. For approval request messages (isApprovalRequest == true), render ApprovalCard widget. For other message types, render a simple message tile.
       - Delete old `signal_detail_screen.dart` and `sse_client.dart`.

    3. **Inbox Provider (MOB-02)**: Rewrite `inbox_provider.dart`:
       - Replace SSE-based InboxNotifier with a simple ChangeNotifier that holds `List<DIDCommMessage>`.
       - `refresh()` calls apiClient.getInbox() and updates the list.
       - Timer-based auto-refresh every 15 seconds when app is in foreground.

    4. **Approval Card (MOB-03)**: Create `features/inbox/approval_card.dart`:
       - `ApprovalCard` StatefulWidget takes a DIDCommMessage (approval request body).
       - If message body has `template_url`: fetch template, verify JWS proof against `via` DID's public key, render branded card using template.brand (name, logo, colors) and template.display (title, fields). Each field rendered according to its type (text, currency, date, etc.).
       - If no template_url: render fallback card showing subject.label and formatted JSON payload.
       - "Approve" and "Decline" buttons:
         - On tap: show `local_auth` biometric prompt.
         - If authenticated: build ApprovalResponse JSON, call `JwsService.signDetached()` with stored private key, call `apiClient.respondApproval(id, status, signature)`.
         - Show success/error snackbar.

    5. **Credentials Screen (MOB-04)**: Create `features/credentials/credentials_screen.dart`:
       - Call `apiClient.getCredentials()` on init.
       - Display list of credentials as cards: type (e.g., "Email Verified"), issued date, revoked status.
       - No revoke action from mobile for v1.0 (server-side only).

    6. **Approvals Screen (MOB-05)**: Create `features/approvals/approvals_screen.dart`:
       - Call `apiClient.getApprovals()` on init, filter to persistent (validUntil != null).
       - Display approval cards with subject label, from/to/via DIDs, valid_until date.
       - "Revoke" button on each: call `apiClient.revokeApproval(id)`, refresh list.

    7. **Navigation (main.dart)**:
       - Update bottom navigation to include: Inbox, Credentials, Approvals, Settings.
       - Delete references to old Signal/SSE screens.
       - Ensure onboarding flow shows first if no DID stored.
       - If `has_recovery_passphrase` is false, show a badge/dot on Settings tab as a reminder.

    8. Delete deprecated files:
       - `mobile/lib/core/models/signal.dart`
       - `mobile/lib/core/api/sse_client.dart`
       - `mobile/lib/features/inbox/signal_detail_screen.dart`
       - `mobile/lib/features/onboarding/claim_screen.dart`
       - `mobile/lib/providers/push_provider.dart` (if exists)
  </action>
  <verify>
    <automated>cd /Users/svenloth/dev/atap/mobile && flutter analyze --no-fatal-infos && flutter test</automated>
  </verify>
  <done>
    App compiles and analyzes cleanly. Onboarding generates keypair, registers DID, then prompts for recovery passphrase (MOB-01 complete). Passphrase encrypts private key backup with PBKDF2-derived AES-256 key. Inbox polls DIDComm messages and renders approval cards (branded with template or fallback). Approve/decline triggers biometric then signs JWS. Credentials screen shows VCs. Approvals screen lists persistent approvals with revoke. No references to old Signal/SSE/push code.
  </done>
</task>

</tasks>

<verification>
- `cd /Users/svenloth/dev/atap/mobile && flutter analyze --no-fatal-infos` — no errors
- `cd /Users/svenloth/dev/atap/mobile && flutter test` — tests pass (if any)
- `cd /Users/svenloth/dev/atap/mobile && flutter build apk --debug` — builds successfully
- No references to `Signal`, `SSEClient`, or `push_provider` in codebase
</verification>

<success_criteria>
- Ed25519 keypair generated and stored securely on first launch
- Entity registered with did:web DID via API
- Recovery passphrase screen shown after registration; passphrase encrypts key backup (MOB-01)
- DIDComm inbox displays messages via polling
- Approval cards render branded (template) or fallback (no template)
- Biometric gate protects approval signing
- JWS detached signature produced from Ed25519 key
- Credentials screen displays VCs
- Approvals screen lists persistent approvals with revoke
- Old Signal/SSE/push code deleted
</success_criteria>

<output>
After completion, create `.planning/phases/04-credentials-and-mobile/04-04-SUMMARY.md`
</output>
