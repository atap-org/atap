---
phase: 01-identity-and-auth-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - platform/internal/api/api.go
  - platform/internal/api/claims.go
  - platform/internal/api/human.go
  - platform/internal/api/push.go
  - platform/internal/api/webhook.go
  - platform/internal/api/api_test.go
  - platform/internal/models/models.go
  - platform/internal/store/store.go
  - platform/internal/crypto/crypto.go
  - platform/cmd/server/main.go
  - platform/migrations/008_strip_old_pipeline.up.sql
  - platform/migrations/008_strip_old_pipeline.down.sql
  - platform/migrations/009_did_and_oauth.up.sql
  - platform/migrations/009_did_and_oauth.down.sql
  - platform/go.mod
  - platform/go.sum
  - docker-compose.yml
autonomous: true
requirements:
  - INF-01
  - INF-02
  - INF-03

must_haves:
  truths:
    - "Old signal/channel/webhook/SSE/claim/delegation code is completely removed"
    - "New database schema has entities with DID columns, key_versions, oauth_clients, oauth_tokens, oauth_auth_codes tables"
    - "Domain model types exist for DIDDocument, OAuthToken, OAuthAuthCode, KeyVersion"
    - "Docker Compose still starts Postgres and Redis successfully"
  artifacts:
    - path: "platform/migrations/008_strip_old_pipeline.up.sql"
      provides: "DROP TABLE for signals, channels, webhook_delivery, claims, delegations, push_tokens"
      contains: "DROP TABLE"
    - path: "platform/migrations/009_did_and_oauth.up.sql"
      provides: "New schema: entity DID columns, key_versions, oauth_auth_codes, oauth_tokens"
      contains: "key_versions"
    - path: "platform/internal/models/models.go"
      provides: "New domain types replacing old signal/channel types"
      contains: "DIDDocument"
    - path: "platform/internal/store/store.go"
      provides: "Store with old methods removed, new entity methods with DID support"
      contains: "did"
    - path: "platform/internal/api/api.go"
      provides: "Handler with old stores removed, slimmed down"
  key_links:
    - from: "platform/migrations/008_strip_old_pipeline.up.sql"
      to: "platform/internal/store/store.go"
      via: "Store methods match surviving schema"
      pattern: "entities"
    - from: "platform/internal/models/models.go"
      to: "platform/internal/store/store.go"
      via: "Store uses new model types"
      pattern: "models\\."
---

<objective>
Strip the old signal pipeline and rebuild the foundation for the new DID/OAuth-based protocol.

Purpose: The existing codebase has signals, channels, webhooks, SSE, claims, and delegations -- all being replaced by DIDComm and OAuth 2.1. This plan removes all of that dead code, creates the new database schema, and establishes the new domain model types that Plans 02-04 build on.

Output: Clean codebase with no old pipeline code, new migration files, updated domain models, and a compilable (but feature-incomplete) platform.
</objective>

<execution_context>
@/Users/svenloth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/svenloth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-identity-and-auth-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strip old pipeline code and create new database migrations</name>
  <files>
    platform/internal/api/claims.go
    platform/internal/api/human.go
    platform/internal/api/push.go
    platform/internal/api/webhook.go
    platform/internal/api/api_test.go
    platform/internal/api/api.go
    platform/internal/models/models.go
    platform/internal/store/store.go
    platform/internal/crypto/crypto.go
    platform/cmd/server/main.go
    platform/migrations/008_strip_old_pipeline.up.sql
    platform/migrations/008_strip_old_pipeline.down.sql
    platform/migrations/009_did_and_oauth.up.sql
    platform/migrations/009_did_and_oauth.down.sql
  </files>
  <behavior>
    - Test: `go build ./...` compiles with zero errors after removing old code
    - Test: Migration 008 drops tables signals, channels, webhook_delivery, claims, delegations, push_tokens
    - Test: Migration 009 adds columns (did, principal_did, client_secret_hash) to entities and creates tables (key_versions, oauth_auth_codes, oauth_tokens) with correct types and constraints
  </behavior>
  <action>
    **Migration 008 (strip_old_pipeline):**
    Create `008_strip_old_pipeline.up.sql`: DROP TABLE IF EXISTS signals, channels, webhook_delivery, claims, delegations, push_tokens CASCADE. Remove `idempotency_key` unique index if it exists.
    Create `008_strip_old_pipeline.down.sql`: Comment saying "irreversible -- re-run migrations 002-007 to restore".

    **Migration 009 (did_and_oauth):**
    Create `009_did_and_oauth.up.sql` with exactly the schema from RESEARCH.md Pattern 4:
    - ALTER TABLE entities ADD COLUMN did TEXT UNIQUE, principal_did TEXT, client_secret_hash TEXT
    - CREATE TABLE key_versions (id TEXT PK, entity_id TEXT FK, public_key BYTEA, key_index INTEGER DEFAULT 1, valid_from TIMESTAMPTZ DEFAULT NOW(), valid_until TIMESTAMPTZ, created_at TIMESTAMPTZ DEFAULT NOW())
    - CREATE TABLE oauth_auth_codes (code TEXT PK, entity_id TEXT FK, redirect_uri TEXT, scope TEXT[], code_challenge TEXT, dpop_jkt TEXT, expires_at TIMESTAMPTZ, used_at TIMESTAMPTZ, created_at TIMESTAMPTZ DEFAULT NOW())
    - CREATE TABLE oauth_tokens (id TEXT PK, entity_id TEXT FK, token_type TEXT DEFAULT 'access', scope TEXT[], dpop_jkt TEXT, expires_at TIMESTAMPTZ, revoked_at TIMESTAMPTZ, created_at TIMESTAMPTZ DEFAULT NOW()) with indexes on (entity_id, token_type) and (expires_at)
    Create matching down migration.

    **Delete files entirely:**
    - `platform/internal/api/claims.go` (claim handlers)
    - `platform/internal/api/human.go` (old human registration via claim)
    - `platform/internal/api/push.go` (push token handlers)
    - `platform/internal/api/webhook.go` (webhook worker + handlers)
    - `platform/internal/api/api_test.go` (old tests for removed handlers)

    **Gut `platform/internal/models/models.go`:**
    Keep: Entity struct (add DID string, PrincipalDID string, ClientSecretHash string fields), EntityType constants, TrustLevel constants, ProblemDetail, RegisterRequest.
    Remove: ALL signal types (Signal, SignalRoute, SignalTrust, SignalBody, SignalContext, SendSignalRequest, InboxResponse), ALL channel types (Channel, CreateChannelRequest, CreateChannelResponse), ALL webhook types (WebhookConfig, SetWebhookRequest, DeliveryAttempt), ALL claim types (Claim, CreateClaimRequest, CreateClaimResponse, RedeemClaimRequest), Delegation, PushToken, HumanRegisterRequest/Response, RegisterResponse (returns private key -- SECURITY VIOLATION in new model).
    Add new types: DIDDocument struct, VerificationMethod struct, KeyVersion struct, OAuthToken struct, OAuthAuthCode struct, CreateEntityRequest struct (type, name, public_key fields), CreateEntityResponse struct (id, did, type fields). See RESEARCH.md for DIDDocument structure.

    **Gut `platform/internal/store/store.go`:**
    Keep: Store struct, New(), Close(), CreateEntity (update to include did, principal_did, client_secret_hash columns), GetEntity (update to scan new columns), GetEntityByKeyID.
    Remove: ALL signal methods (SaveSignal, GetSignal, GetInbox, GetSignalsAfter), ALL channel methods, ALL webhook methods, ALL claim methods, ALL delegation methods, ALL push token methods, ErrDuplicateSignal.
    Add: GetEntityByDID(ctx, did) method, DeleteEntity(ctx, id) method (for API-01 crypto-shred -- just DELETE FROM entities WHERE id=$1 for now).

    **Update `platform/internal/api/api.go`:**
    Remove: SignalStore, ChannelStore, WebhookStore, ClaimStore, DelegationStore, PushTokenStore, PushNotifier interfaces. Remove all Set*Store methods. Remove ALL handler methods EXCEPT Health() and problem() helper. Remove RegisterAgent, RegisterHuman, GetEntity, GetMe, SendSignal, GetInbox, InboxStream, SetWebhook, CreateChannel, ListChannels, RevokeChannel, ChannelInbound, AuthMiddleware.
    Update Handler struct: Keep entityStore, config, redis, platformKey, log. Remove signalStore, channelStore, webhookStore, claimStore, delegationStore, pushTokenStore, webhookWorker, pushService.
    Update NewHandler to accept only (EntityStore, *redis.Client, ed25519.PrivateKey, *config.Config, zerolog.Logger).
    Update SetupRoutes to only have `v1.Get("/health", h.Health)` as a placeholder.

    **Update `platform/internal/crypto/crypto.go`:**
    Keep: GenerateKeyPair, Sign, Verify, DeriveHumanID, NewEntityID, CanonicalJSON, EncodePublicKey, DecodePublicKey.
    Remove: NewSignalID, NewDeliveryAttemptID, NewChannelID, NewClaimID, NewDelegationID, GenerateClaimCode, SignRequest, VerifyRequest, ParseSignatureHeader, splitParams, MaxTimestampSkew, EncodePrivateKey (SECURITY: server must never encode private keys), SignablePayload.

    **Update `platform/cmd/server/main.go`:**
    Remove: firebase import, push package import, WebhookWorker setup, Firebase initialization block, all Set*Store calls.
    Update: NewHandler call signature to match reduced constructor. Remove push and webhook worker references.
    Keep: Database, Redis, migrations, Fiber setup, graceful shutdown.

    **Update `platform/go.mod`:**
    Run `go mod tidy` to remove unused dependencies (firebase, google API).
    Add `github.com/mr-tron/base58` (needed for multibase encoding in Plan 02).
    Add `github.com/AxisCommunications/go-dpop` (needed for DPoP in Plan 04).
    Promote `github.com/go-jose/go-jose/v4` from indirect to direct.
  </action>
  <verify>
    <automated>cd /Users/svenloth/dev/atap/platform && go build ./...</automated>
  </verify>
  <done>
    - Files claims.go, human.go, push.go, webhook.go deleted
    - api.go has only Health handler + problem helper + slim Handler struct
    - models.go has Entity (with DID fields), DIDDocument, KeyVersion, OAuth types, ProblemDetail -- no signal/channel/webhook/claim/delegation types
    - store.go has entity CRUD only -- no signal/channel/webhook/claim/delegation methods
    - crypto.go has no SignRequest/VerifyRequest/ParseSignatureHeader (old auth)
    - main.go compiles with slimmed dependencies
    - Migrations 008 and 009 exist with correct SQL
    - `go build ./...` succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify Docker Compose and migration application</name>
  <files>
    docker-compose.yml
  </files>
  <action>
    1. Review `docker-compose.yml` -- ensure Postgres and Redis services are still correctly configured. The platform service may need its Dockerfile updated if it references deleted files. Update the Dockerfile if needed (check `platform/Dockerfile`).
    2. Run `docker compose up -d postgres redis` to start infrastructure.
    3. Run `cd platform && go test ./...` to confirm the test suite passes (should be minimal/empty tests at this point).
    4. Verify the platform binary builds and can start against the database: `cd platform && go build -o ../bin/atap-platform ./cmd/server && timeout 5 ../bin/atap-platform || true` (it will start and we kill after 5s).
    5. If Docker Compose has a `platform` service definition, update it to remove any environment variables related to Firebase/push notifications.
  </action>
  <verify>
    <automated>cd /Users/svenloth/dev/atap/platform && go build ./... && go vet ./...</automated>
  </verify>
  <done>
    - Docker Compose starts Postgres and Redis without errors
    - `go build ./...` and `go vet ./...` pass cleanly
    - Platform binary builds successfully
    - No references to deleted code remain in build artifacts
  </done>
</task>

</tasks>

<verification>
- `cd platform && go build ./...` compiles without errors
- `cd platform && go vet ./...` reports no issues
- `grep -r "Signal\|Channel\|Webhook\|Claim\|Delegation\|PushToken" platform/internal/ --include="*.go" | grep -v "_test.go" | grep -v "models.go"` returns no matches (old types fully removed except where models.go re-defines needed types)
- Migration files 008 and 009 exist and contain valid SQL
</verification>

<success_criteria>
The platform compiles cleanly with all old signal pipeline code removed. The new database schema (migrations 008 + 009) is ready. Domain model types for DIDs, OAuth, and key versions exist. The codebase is a clean foundation for Plans 02-04.
</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-and-auth-foundation/01-01-SUMMARY.md`
</output>
