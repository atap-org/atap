---
phase: 01-foundation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - platform/go.mod
  - platform/go.sum
  - platform/Dockerfile
  - docker-compose.yml
  - platform/internal/config/config.go
  - platform/internal/crypto/crypto.go
  - platform/internal/crypto/crypto_test.go
  - platform/internal/models/models.go
  - platform/migrations/001_entities.up.sql
  - platform/migrations/001_entities.down.sql
autonomous: true
requirements:
  - CRY-01
  - CRY-02
  - CRY-03
  - CRY-04
  - INF-01
  - INF-02
  - INF-03
  - INF-06
  - TST-03
  - TST-04
  - REG-02
  - REG-03
  - REG-05

must_haves:
  truths:
    - "Ed25519 keypair generation produces valid 32-byte public + 64-byte private keys"
    - "CanonicalJSON produces RFC 8785 compliant output (not just json.Marshal)"
    - "SignablePayload creates JCS(route).JCS(signal) format"
    - "Channel IDs use 128-bit entropy (chn_ + 32 hex chars)"
    - "Tokens use atap_ prefix + 32 bytes base64url, and hash verification round-trips"
    - "Entity IDs are lowercase ULIDs"
    - "go build succeeds with updated Go 1.24 and all dependencies"
    - "Docker image builds successfully"
    - "Migration file creates only the entities table"
  artifacts:
    - path: "platform/internal/crypto/crypto.go"
      provides: "Ed25519, JCS, tokens, IDs"
      exports: ["GenerateKeyPair", "Sign", "Verify", "CanonicalJSON", "SignablePayload", "NewToken", "HashToken", "NewEntityID", "NewChannelID", "NewKeyID", "EncodePublicKey", "EncodePrivateKey", "DecodePublicKey"]
    - path: "platform/internal/crypto/crypto_test.go"
      provides: "Unit tests for all crypto functions"
      min_lines: 100
    - path: "platform/internal/models/models.go"
      provides: "Entity, RegisterRequest, RegisterResponse (with private_key), EntityLookupResponse, ProblemDetail"
      contains: "PrivateKey"
    - path: "platform/internal/config/config.go"
      provides: "Phase 1 config (no SIMRelay, WorldID, FCM fields)"
    - path: "platform/migrations/001_entities.up.sql"
      provides: "Entities table only"
    - path: "platform/migrations/001_entities.down.sql"
      provides: "Drop entities table"
    - path: "platform/Dockerfile"
      provides: "Multi-stage Alpine build with correct binary name"
      contains: "atap-platform"
    - path: "docker-compose.yml"
      provides: "platform + postgres + redis"
    - path: "platform/go.mod"
      provides: "Go 1.24 with updated dependencies including gowebpki/jcs and golang-migrate"
  key_links:
    - from: "platform/internal/crypto/crypto.go"
      to: "github.com/gowebpki/jcs"
      via: "jcs.Transform() in CanonicalJSON"
      pattern: "jcs\\.Transform"
    - from: "platform/internal/crypto/crypto_test.go"
      to: "platform/internal/crypto/crypto.go"
      via: "import and test all exported functions"
      pattern: "crypto\\."
---

<objective>
Infrastructure, crypto primitives, and domain models for Phase 1.

Purpose: Establish the buildable foundation -- updated Go module, fixed crypto (JCS, channel entropy), trimmed models, entities-only migration, and working Docker setup. All downstream code (store, API, handlers) depends on these artifacts.

Output: Compiling Go module with all dependencies, passing crypto unit tests, buildable Docker image, entities-only migration.
</objective>

<execution_context>
@/Users/svenloth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/svenloth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md

@platform/go.mod
@platform/internal/crypto/crypto.go
@platform/internal/models/models.go
@platform/internal/config/config.go
@platform/Dockerfile
@docker-compose.yml
@platform/migrations/001_init.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Go module, dependencies, Dockerfile, Docker Compose, migration, config, and models</name>
  <files>
    platform/go.mod, platform/go.sum,
    platform/Dockerfile,
    docker-compose.yml,
    platform/internal/config/config.go,
    platform/internal/models/models.go,
    platform/migrations/001_entities.up.sql, platform/migrations/001_entities.down.sql
  </files>
  <action>
    **go.mod:** Update `go 1.22` to `go 1.24`. Run the following to update all dependencies and add new ones:
    ```
    cd platform
    go mod edit -go=1.24
    go get github.com/gofiber/fiber/v2@latest
    go get github.com/jackc/pgx/v5@v5.7.5
    go get github.com/redis/go-redis/v9@latest
    go get github.com/rs/zerolog@latest
    go get github.com/oklog/ulid/v2@latest
    go get github.com/gowebpki/jcs@latest
    go get github.com/golang-migrate/migrate/v4@latest
    go get github.com/golang-migrate/migrate/v4/database/postgres@latest
    go get github.com/golang-migrate/migrate/v4/source/file@latest
    go mod tidy
    ```

    **Dockerfile:** Rewrite platform/Dockerfile:
    - Change `golang:1.22-alpine` to `golang:1.24-alpine`
    - Fix binary name from `atep-platform` to `atap-platform` (typo fix)
    - Add `COPY migrations/ ./migrations/` before the runtime stage copy so migrations are available
    - Runtime stage: copy both binary and migrations directory
    - CMD: `["./atap-platform"]`

    **docker-compose.yml:** Rewrite:
    - Remove deprecated `version: "3.9"` key
    - Remove `volumes: - ./platform:/app` from platform service (production image, not dev mount)
    - Remove the `./platform/migrations:/docker-entrypoint-initdb.d` volume from postgres (migrations will be run by golang-migrate at startup, not by initdb)
    - Keep postgres healthcheck, redis healthcheck, pgdata volume
    - Keep all environment variables as-is

    **config.go:** Trim to Phase 1 fields only:
    - Keep: Port, Host, DatabaseURL, RedisURL, PlatformDomain
    - Remove: SIMRelayAPIURL, SIMRelayAPIKey, SIMRelayWebhookSecret, SIMRelayNumber, WorldIDAppID, WorldIDRPID, WorldIDRPSigningKey, FCMCredentialsJSON
    - Add: MigrationsPath with default `"migrations"` (for golang-migrate file source)

    **models.go:** Trim to Phase 1 only:
    - Keep: Entity type constants (EntityTypeAgent, etc.), TrustLevel constants, Entity struct, RegisterRequest, RegisterResponse, ProblemDetail
    - Remove: Delivery preference constants, Signal/SignalRoute/SignalBody/SignalContext/SignalTrust/SignalEncryption structs, Channel struct, Delegation/DelegationScope/DelegationConstraints structs, Claim struct, ApprovalTemplate struct, SendSignalRequest struct
    - Entity struct: Remove Phase 2+ fields (PublicKeyX25519, Description, DeliveryPref, WebhookURL, PushToken, PushPlatform, OwnerID, OrgID, Attestations, RecoveryBackup, RevocationURL, DeletedAt). Keep: ID, Type, URI, PublicKeyEd25519, KeyID, PublicKeyBase64 (omitempty), Name, TrustLevel, TokenHash (json:"-"), Registry, CreatedAt, UpdatedAt
    - RegisterRequest: Keep Name only. Remove Description, DeliveryPref, WebhookURL (Phase 2 fields)
    - RegisterResponse: Change to match CONTEXT.md locked decision: URI, ID, Token, PublicKey, PrivateKey, KeyID. No InboxURL, no StreamURL. Add `PrivateKey string json:"private_key"`
    - Add EntityLookupResponse struct per RESEARCH.md: ID, Type, URI, PublicKey, KeyID, Name (omitempty), TrustLevel, Registry, CreatedAt

    **Migration files:**
    - Delete platform/migrations/001_init.sql (the old all-tables migration)
    - Create platform/migrations/001_entities.up.sql with ONLY the entities table per RESEARCH.md code example. Columns: id (TEXT PK), type (TEXT NOT NULL with CHECK), uri (TEXT UNIQUE NOT NULL), public_key_ed25519 (BYTEA NOT NULL), key_id (TEXT NOT NULL), name (TEXT), trust_level (INTEGER NOT NULL DEFAULT 0 CHECK 0-3), token_hash (BYTEA NOT NULL), registry (TEXT NOT NULL DEFAULT 'atap.app'), created_at (TIMESTAMPTZ NOT NULL DEFAULT NOW()), updated_at (TIMESTAMPTZ NOT NULL DEFAULT NOW()). Indexes: idx_entities_type on type, idx_entities_token on token_hash. Include `CREATE EXTENSION IF NOT EXISTS "pgcrypto"`.
    - Create platform/migrations/001_entities.down.sql: `DROP TABLE IF EXISTS entities; DROP EXTENSION IF EXISTS "pgcrypto";`

    Clean up empty Phase 2+ directories:
    ```
    rm -rf platform/internal/delivery platform/internal/federation platform/internal/integrations platform/internal/verify
    ```
  </action>
  <verify>
    <automated>cd /Users/svenloth/dev/atap/platform && go build ./... 2>&1 | head -20</automated>
  </verify>
  <done>
    - go.mod shows go 1.24 with gowebpki/jcs and golang-migrate dependencies
    - go.sum exists and is valid
    - `go build ./...` succeeds (may have unused import warnings from api/store not yet trimmed -- that is OK, they are fixed in Plan 02)
    - Dockerfile uses golang:1.24-alpine and binary name atap-platform
    - docker-compose.yml has no version key, no dev volume mount
    - config.go has no Phase 2+ fields
    - models.go has PrivateKey in RegisterResponse, EntityLookupResponse struct, no Signal/Channel/Delegation types
    - 001_entities.up.sql creates only entities table
    - 001_entities.down.sql drops entities table
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crypto module -- fix CanonicalJSON (JCS), channel entropy, add EncodePrivateKey, write tests</name>
  <files>platform/internal/crypto/crypto.go, platform/internal/crypto/crypto_test.go</files>
  <behavior>
    - TestGenerateKeyPair: returns 32-byte public key, 64-byte private key, no error
    - TestSign_Verify: sign data with private key, verify with public key returns true, verify with wrong data returns false
    - TestCanonicalJSON: produces RFC 8785 compliant output (sorted keys, no extra whitespace). Test with a map containing nested objects and verify output is deterministic
    - TestCanonicalJSON_FloatHandling: verify that floats are serialized per ECMAScript spec (the key difference from json.Marshal). Test with 1e20, 10.0, etc.
    - TestSignablePayload: given route and signal maps, produces "JCS(route).JCS(signal)" format with dot separator
    - TestNewToken: token starts with "atap_", has correct length (~49 chars: 5 prefix + 43 base64url), different calls produce different tokens
    - TestHashToken: HashToken(token) produces 32-byte SHA-256 hash, same token always produces same hash
    - TestTokenRoundTrip: NewToken returns token+hash, HashToken(token) equals the returned hash
    - TestNewEntityID: produces lowercase string of 26 chars (ULID), different calls produce different IDs
    - TestNewChannelID: starts with "chn_", followed by exactly 32 hex chars (128-bit entropy), different calls produce different IDs
    - TestNewKeyID: starts with "key_{prefix}_", followed by 8 hex chars
    - TestEncodeDecodePublicKey: encode then decode round-trips successfully
    - TestEncodePrivateKey: encodes 64-byte private key to base64 standard encoding
    - TestDeriveHumanID: produces lowercase 16-char string from public key, same key always produces same ID
  </behavior>
  <action>
    **RED phase:** Write crypto_test.go with all tests listed in behavior. Tests should import the crypto package and call functions directly. Use Go stdlib testing only (no testify). Use table-driven tests where appropriate (e.g., group TestNewToken length/prefix/uniqueness as subtests). Run tests -- they should fail for CanonicalJSON (uses json.Marshal not JCS) and NewChannelID (only 16 hex chars, not 32).

    **GREEN phase:** Update crypto.go:
    1. Add import `"github.com/gowebpki/jcs"`
    2. Replace CanonicalJSON body: first json.Marshal(v), then jcs.Transform(raw), return canonical bytes. Wrap errors with context.
    3. Fix NewChannelID: change `make([]byte, 8)` to `make([]byte, 16)` for 128-bit entropy producing 32 hex chars
    4. Add EncodePrivateKey function: `func EncodePrivateKey(key ed25519.PrivateKey) string { return base64.StdEncoding.EncodeToString(key) }`
    5. Remove Phase 2+ functions that are not needed: NewSignalID, NewDelegationID, NewClaimID, NewClaimCode (these create IDs for types that do not exist in Phase 1). Keep DeriveHumanID (it is a crypto primitive useful for testing).

    Run tests -- all should pass.

    **REFACTOR:** Clean up any redundant code. Ensure all exported functions have doc comments.
  </action>
  <verify>
    <automated>cd /Users/svenloth/dev/atap/platform && go test ./internal/crypto/ -v -count=1</automated>
  </verify>
  <done>
    - All crypto tests pass including JCS canonical JSON and 128-bit channel ID
    - CanonicalJSON uses gowebpki/jcs.Transform (not plain json.Marshal)
    - NewChannelID produces "chn_" + 32 hex chars
    - EncodePrivateKey exists and is tested
    - No Phase 2+ ID generators remain (NewSignalID, NewDelegationID, NewClaimID, NewClaimCode removed)
  </done>
</task>

</tasks>

<verification>
```bash
# All crypto tests pass
cd /Users/svenloth/dev/atap/platform && go test ./internal/crypto/ -v -count=1

# Module builds cleanly
cd /Users/svenloth/dev/atap/platform && go build ./cmd/server/ 2>&1 | head -5

# Dockerfile builds (optional -- requires Docker running)
# docker build -t atap-platform ./platform
```
</verification>

<success_criteria>
- Go 1.24 module with all dependencies resolves and builds
- Crypto unit tests pass for keypair gen, JCS canonical JSON, signing, token gen/hash, channel ID entropy
- Models trimmed to Phase 1 (Entity, RegisterResponse with private_key, EntityLookupResponse)
- Config trimmed (no SIMRelay, WorldID, FCM fields)
- Migration creates only entities table
- Dockerfile produces correct binary name (atap-platform)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
