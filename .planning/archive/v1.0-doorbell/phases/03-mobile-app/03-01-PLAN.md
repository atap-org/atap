---
phase: 03-mobile-app
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - platform/internal/models/models.go
  - platform/internal/store/store.go
  - platform/migrations/005_claims.up.sql
  - platform/migrations/005_claims.down.sql
  - platform/migrations/006_delegations.up.sql
  - platform/migrations/006_delegations.down.sql
  - platform/migrations/007_push_tokens.up.sql
  - platform/migrations/007_push_tokens.down.sql
autonomous: true
requirements:
  - MOB-04

must_haves:
  truths:
    - "Claims, delegations, and push_tokens tables exist in PostgreSQL"
    - "Store can create and query claims, delegations, and push tokens"
    - "All existing tests continue to pass (no regressions)"
  artifacts:
    - path: "platform/migrations/005_claims.up.sql"
      provides: "Claims table with id, code, creator_id, redeemed_by, status, timestamps"
      contains: "CREATE TABLE claims"
    - path: "platform/migrations/006_delegations.up.sql"
      provides: "Delegations table with delegator/delegate, scope, timestamps"
      contains: "CREATE TABLE delegations"
    - path: "platform/migrations/007_push_tokens.up.sql"
      provides: "Push tokens table with entity_id, token, platform"
      contains: "CREATE TABLE push_tokens"
    - path: "platform/internal/models/models.go"
      provides: "Claim, Delegation, PushToken, HumanRegisterRequest structs"
      contains: "type Claim struct"
    - path: "platform/internal/store/store.go"
      provides: "Store methods for claims, delegations, push tokens"
      exports: ["CreateClaim", "GetClaimByCode", "RedeemClaim", "CreateDelegation", "UpsertPushToken", "GetPushToken"]
  key_links:
    - from: "platform/internal/store/store.go"
      to: "platform/internal/models/models.go"
      via: "models.Claim, models.Delegation, models.PushToken types"
      pattern: "models\\.(Claim|Delegation|PushToken)"
---

<objective>
Add the data layer for Phase 3 mobile features: database migrations for claims, delegations, and push tokens; domain models; and store methods.

Purpose: Platform needs claims (invite flow), delegations (human-to-agent trust chain), and push tokens (notification delivery) before API endpoints can be built.
Output: Three new migrations (005-007), extended models, and store methods.
</objective>

<execution_context>
@/Users/svenloth/.claude/get-shit-done/workflows/execute-plan.md
@/Users/svenloth/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-mobile-app/03-CONTEXT.md
@.planning/phases/03-mobile-app/03-RESEARCH.md

<interfaces>
<!-- Existing platform interfaces the executor needs -->

From platform/internal/models/models.go:
```go
type Entity struct {
    ID        string    `json:"id"`
    Type      string    `json:"type"`
    URI       string    `json:"uri"`
    PublicKey string    `json:"public_key"`
    KeyID     string    `json:"key_id"`
    Metadata  string    `json:"metadata,omitempty"`
    CreatedAt time.Time `json:"created_at"`
}
```

From platform/internal/store/store.go (existing pattern):
```go
func (s *Store) CreateEntity(ctx context.Context, e *models.Entity) error { ... }
func (s *Store) GetEntity(ctx context.Context, id string) (*models.Entity, error) { ... }
```

From platform/internal/crypto/crypto.go:
```go
func DeriveHumanID(publicKey ed25519.PublicKey) string { ... }
func NewEntityID() string { ... }
```

Existing ID conventions from crypto.go:
- NewSignalID: `sig_` + ULID
- NewChannelID: `chn_` + 32 hex chars
- Need to add: NewClaimID (`clm_` + 12 hex), NewDelegationID (`del_` + 12 hex), GenerateClaimCode (`ATAP-` + 4 alphanum)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migrations and extend models</name>
  <files>
    platform/migrations/005_claims.up.sql,
    platform/migrations/005_claims.down.sql,
    platform/migrations/006_delegations.up.sql,
    platform/migrations/006_delegations.down.sql,
    platform/migrations/007_push_tokens.up.sql,
    platform/migrations/007_push_tokens.down.sql,
    platform/internal/models/models.go,
    platform/internal/crypto/crypto.go
  </files>
  <action>
    1. Create migration 005_claims:
       - UP: CREATE TABLE claims (id TEXT PRIMARY KEY, code TEXT UNIQUE NOT NULL, creator_id TEXT NOT NULL REFERENCES entities(id), redeemed_by TEXT REFERENCES entities(id), status TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('pending', 'redeemed', 'expired')), created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), redeemed_at TIMESTAMPTZ, expires_at TIMESTAMPTZ). Indexes on code and creator_id.
       - DOWN: DROP TABLE claims

    2. Create migration 006_delegations:
       - UP: CREATE TABLE delegations (id TEXT PRIMARY KEY, delegator_id TEXT NOT NULL REFERENCES entities(id), delegate_id TEXT NOT NULL REFERENCES entities(id), scope JSONB DEFAULT '[]', created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), revoked_at TIMESTAMPTZ, UNIQUE(delegator_id, delegate_id)). Indexes on delegator_id and delegate_id.
       - DOWN: DROP TABLE delegations

    3. Create migration 007_push_tokens:
       - UP: CREATE TABLE push_tokens (entity_id TEXT PRIMARY KEY REFERENCES entities(id) ON DELETE CASCADE, token TEXT NOT NULL, platform TEXT NOT NULL CHECK (platform IN ('android', 'ios')), created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()). Index on token.
       - DOWN: DROP TABLE push_tokens

    4. Add models to models.go:
       - Claim struct: ID, Code, CreatorID, RedeemedBy (*string), Status, CreatedAt, RedeemedAt (*time.Time), ExpiresAt (*time.Time) with json tags
       - Delegation struct: ID, DelegatorID, DelegateID, Scope (json.RawMessage), CreatedAt, RevokedAt (*time.Time) with json tags
       - PushToken struct: EntityID, Token, Platform, CreatedAt, UpdatedAt with json tags
       - HumanRegisterRequest: PublicKey, Email, ClaimCode with json tags
       - HumanRegisterResponse: Entity, KeyID with json tags
       - CreateClaimRequest: empty struct (agent creates claim, no input needed beyond auth)
       - CreateClaimResponse: ID, Code, Link with json tags
       - RedeemClaimRequest: PublicKey, Email with json tags

    5. Add to crypto.go:
       - NewClaimID() string: returns "clm_" + 12 hex chars (6 random bytes)
       - NewDelegationID() string: returns "del_" + 12 hex chars (6 random bytes)
       - GenerateClaimCode() string: returns "ATAP-" + 4 alphanumeric chars (random)
  </action>
  <verify>
    <automated>cd /Users/svenloth/dev/atap/platform && go build ./...</automated>
  </verify>
  <done>All 6 migration files exist. Claim, Delegation, PushToken models compile. NewClaimID, NewDelegationID, GenerateClaimCode functions exist in crypto.go.</done>
</task>

<task type="auto">
  <name>Task 2: Implement store methods for claims, delegations, and push tokens</name>
  <files>
    platform/internal/store/store.go
  </files>
  <action>
    Add store methods following existing patterns (use pgx pool, wrap errors with context):

    Claims:
    - CreateClaim(ctx, claim *models.Claim) error -- INSERT, return wrapped error
    - GetClaimByCode(ctx, code string) (*models.Claim, error) -- SELECT by code, return nil+nil if not found (pgx.ErrNoRows)
    - RedeemClaim(ctx, code string, redeemedBy string) error -- UPDATE SET status='redeemed', redeemed_by=$2, redeemed_at=NOW() WHERE code=$1 AND status='pending'. Return error if no rows affected (claim not found or already redeemed).

    Delegations:
    - CreateDelegation(ctx, del *models.Delegation) error -- INSERT, return wrapped error
    - GetDelegationsByDelegator(ctx, delegatorID string) ([]*models.Delegation, error) -- SELECT WHERE delegator_id=$1 AND revoked_at IS NULL
    - GetDelegationsByDelegate(ctx, delegateID string) ([]*models.Delegation, error) -- SELECT WHERE delegate_id=$1 AND revoked_at IS NULL

    Push Tokens:
    - UpsertPushToken(ctx, entityID, token, platform string) error -- INSERT ON CONFLICT (entity_id) DO UPDATE SET token=$2, platform=$3, updated_at=NOW()
    - GetPushToken(ctx, entityID string) (*models.PushToken, error) -- SELECT by entity_id, return nil+nil if not found
    - DeletePushToken(ctx, entityID string) error -- DELETE by entity_id

    Follow existing scan pattern: create private scanClaim, scanDelegation helper functions that take pgx.Row and return the model pointer.
  </action>
  <verify>
    <automated>cd /Users/svenloth/dev/atap/platform && go test ./... -count=1</automated>
  </verify>
  <done>All store methods compile and existing tests pass. CreateClaim, GetClaimByCode, RedeemClaim, CreateDelegation, GetDelegationsByDelegator, GetDelegationsByDelegate, UpsertPushToken, GetPushToken, DeletePushToken methods exist on Store.</done>
</task>

</tasks>

<verification>
- `cd platform && go build ./...` succeeds
- `cd platform && go test ./... -count=1` passes (no regressions)
- Migration files 005-007 exist with correct SQL
- models.go contains Claim, Delegation, PushToken structs
- store.go contains all new methods
</verification>

<success_criteria>
Platform data layer is complete: 3 new migrations, 3 new model types, 9 new store methods. All existing tests pass. Ready for API endpoint implementation in Plan 03.
</success_criteria>

<output>
After completion, create `.planning/phases/03-mobile-app/03-01-SUMMARY.md`
</output>
